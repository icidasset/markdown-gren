module Markdown.Parser.Frontmatter.Types exposing ( Parser )

{-| Frontmatter types.

@docs Parser

-}

import Parser


{-| Frontmatter parser type.
-}
type alias Parser m =
    Parser.Parser
        { metadata : Maybe m
        , markdown : String
        }
